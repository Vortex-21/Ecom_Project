<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ecom</title>
</head>
<body>
    <!-- Card for listing required -->
    <ul>
        <li><%=listing.name%></li>
        <li>&#8377;<%=listing.price.toLocaleString("en-IN")%></li>
        <li><%=listing.description%></li>
        <form action="/listings/<%=listing._id%>/edit">
            <button>Edit</button>
        </form>
    </ul>
</body>
</html>